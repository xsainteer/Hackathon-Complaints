version: '3.8'

services:
  postgres:
    image: postgres
    container_name: complaints
    restart: always
    environment:
      POSTGRES_USER: xsainteer
      POSTGRES_PASSWORD: password
      POSTGRES_DB: complaints
    ports:
      - "5432:5432"